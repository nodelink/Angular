function StoreController(){
alert('Welcome, Rembo!')
}

//var app = angular.module('store', [ ]);

            // A closure:
(function(){var app = angular.module('store', [ ]);

            // controller:
    app.controller('StoreController', function(){
    this.product = gem;});

            // object to print to the page:
    var gem = {
        name: 'Dodecahedron',
        price: 2.95,
        description: 'If a fiddle is loaded clicking on Update will create a new version of the fiddle and load it into the browser. Its URL will now contain a version number. This and former versions are still available.',
}

})();
